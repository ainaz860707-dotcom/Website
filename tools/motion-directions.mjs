export const MOTION_GROUPS = [
  { key: 'function', title: 'Функциональные — помогают пользователю' },
  { key: 'scroll', title: 'Скролл — страница разворачивается по мере чтения' },
  { key: 'decor', title: 'Декоративные и атмосферные — держат настроение' },
];

export const TECHNIQUES = [
  {
    key: 'micro',
    group: 'function',
    name: 'Микроинтеракции',
    trigger: 'hover · focus · нажатие',
    tech: 'CSS transition',
    duration: 'реактивная',
    cost: '0 КБ',
    crawlerRisk: 'нет',
    rule: `Кнопки, ссылки, карточки и поля отзываются на hover, focus-visible и active:
  трансформация и цвет по --dur-hover, кривая --ease-ui; нажатие сжимает до --press.`,
  },
  {
    key: 'state',
    group: 'function',
    name: 'Переходы состояний',
    trigger: 'клик · открытие',
    tech: 'CSS transition, details/summary',
    duration: 'реактивная',
    cost: '0 КБ',
    crawlerRisk: 'скрытый контент — только через <details>',
    rule: `Раскрытие показывает, откуда взялся элемент: у аккордеона поворачивается маркер и
  разъезжается содержимое, выпадающее появляется от своей кнопки, а не из ниоткуда.
  Аккордеон вопросов — на <details>/<summary>, его содержимое остаётся в разметке всегда.`,
  },
  {
    key: 'feedback',
    group: 'function',
    name: 'Обратная связь',
    trigger: 'отправка формы · ошибка',
    tech: 'CSS + минимум JS',
    duration: 'одноразовая',
    cost: '0 КБ',
    crawlerRisk: 'нет',
    rule: `Форма отвечает человеку: подсветка неверного поля, состояние «отправляется»,
  сообщение об успехе или ошибке появляется рядом с кнопкой, а не заменяет страницу.`,
  },
  {
    key: 'loading',
    group: 'function',
    name: 'Загрузка',
    trigger: 'ожидание',
    tech: 'CSS keyframes',
    duration: 'непрерывная до конца ожидания',
    cost: '0 КБ',
    crawlerRisk: 'скелетон поверх текста — запрещён',
    rule: `Ожидание показано скелетоном или прогрессом в формах и тяжёлых блоках. Скелетон
  не подменяет собой готовый текст страницы: смысловой текст отрисован сразу.`,
  },
  {
    key: 'nav',
    group: 'function',
    name: 'Навигация',
    trigger: 'клик · скролл',
    tech: 'CSS + класс на скролле',
    duration: 'реактивная',
    cost: '0 КБ',
    crawlerRisk: 'меню обязано быть в разметке ссылками',
    rule: `Шапка живёт: на отрыве от верха уплотняется и получает тень, при скролле вверх
  возвращается. Мобильное меню разворачивается на весь экран, гамбургер перетекает в крестик.
  Ссылки меню лежат в разметке настоящими <a> и без скрипта.`,
  },
  {
    key: 'page',
    group: 'function',
    name: 'Переходы между страницами',
    trigger: 'переход по ссылке',
    tech: 'View Transitions API',
    duration: 'одноразовая',
    cost: '0 КБ',
    crawlerRisk: 'нет — деградирует сама',
    rule: `Переходы между страницами — на View Transitions API через @view-transition,
  с общим элементом там, где карточка раскрывается в страницу. Браузер без поддержки
  переходит обычным способом.`,
  },
  {
    key: 'intro',
    group: 'function',
    name: 'Интро первого экрана',
    trigger: 'загрузка',
    tech: 'CSS keyframes с задержками',
    duration: 'одноразовая',
    cost: '0 КБ',
    crawlerRisk: 'высокий — только под классом .js',
    rule: `Вход первого экрана срежиссирован: ступенчатые задержки по --stagger между заголовком,
  подводкой, кнопкой и строкой фактов. Прелоадер во весь экран не ставится: он крадёт
  первые секунды и первый экран у поиска.`,
  },
  {
    key: 'gallery',
    group: 'function',
    name: 'Слайдеры и галереи',
    trigger: 'drag · клик · скролл',
    tech: 'CSS scroll-snap',
    duration: 'реактивная',
    cost: '0 КБ',
    crawlerRisk: 'нет при scroll-snap',
    rule: `Галерея — лента на CSS scroll-snap с инерцией браузера, а не карусель на скрипте:
  все кадры лежат в разметке, листается пальцем и колесом, клавиатура работает.`,
  },
  {
    key: 'reveal',
    group: 'scroll',
    name: 'Появление по скроллу',
    trigger: 'попадание во вьюпорт',
    tech: 'IntersectionObserver + transition',
    duration: '--dur-reveal, каскад через --stagger',
    cost: '0 КБ',
    crawlerRisk: 'высокий — только под классом .js',
    rule: `Секции проявляются при подходе к экрану: наблюдатель вешает класс .is-in, переход
  идёт по --dur-reveal с задержкой calc(var(--i) * var(--stagger)). Способ на голом CSS
  (animation-timeline: view()) не берётся: где свойство не поддержано, оно игнорируется и
  страница выдаёт все блоки разом, без каскада. Рецепт целиком — в правиле скрытия ниже.`,
  },
  {
    key: 'parallax',
    group: 'scroll',
    name: 'Параллакс',
    trigger: 'прогресс скролла',
    tech: 'animation-timeline: scroll()',
    duration: 'управляемая прогрессом',
    cost: '0 КБ',
    crawlerRisk: 'нет',
    rule: `Слои двигаются с разной скоростью через transform на animation-timeline: scroll().
  Смещение сдержанное — десятки пикселей, не половина экрана: от сильного параллакса
  у части людей кружится голова.`,
  },
  {
    key: 'pinned',
    group: 'scroll',
    name: 'Закреплённая сцена',
    trigger: 'прогресс скролла',
    tech: 'position: sticky + scroll()',
    duration: 'управляемая прогрессом',
    cost: '0 КБ',
    crawlerRisk: 'средний — содержимое сцены остаётся в разметке',
    rule: `Одна секция закрепляется на position: sticky, и внутри неё по прогрессу скролла
  сменяются кадры или шаги. Все кадры лежат в разметке и читаются подряд без прокрутки.`,
  },
  {
    key: 'horizontal',
    group: 'scroll',
    name: 'Горизонтальный ход',
    trigger: 'прогресс скролла',
    tech: 'sticky + translateX',
    duration: 'управляемая прогрессом',
    cost: '0 КБ',
    crawlerRisk: 'средний',
    rule: `Один блок уходит вбок по мере вертикального скролла. Ровно один такой блок на
  страницу, и на ширине до 640px он превращается в обычную вертикальную ленту.`,
  },
  {
    key: 'progress',
    group: 'scroll',
    name: 'Прогресс чтения',
    trigger: 'прогресс скролла',
    tech: 'animation-timeline: scroll(root)',
    duration: 'управляемая прогрессом',
    cost: '0 КБ',
    crawlerRisk: 'нет',
    rule: `Полоса прогресса чтения или подсветка текущего раздела в шапке — на
  animation-timeline: scroll(root), без обработчика скролла на JS.`,
  },
  {
    key: 'story',
    group: 'scroll',
    name: 'Скроллителлинг',
    trigger: 'прогресс скролла',
    tech: 'sticky + scroll() + canvas или SVG',
    duration: 'управляемая прогрессом',
    cost: '0 КБ на CSS',
    crawlerRisk: 'высокий — текст нарратива обязан быть в разметке',
    rule: `Длинный нарратив: текст идёт колонкой, графика рядом закреплена и меняется по ходу
  чтения. Весь текст нарратива лежит в HTML подряд и читается без прокрутки.`,
  },
  {
    key: 'kinetic',
    group: 'decor',
    name: 'Кинетическая типографика',
    trigger: 'загрузка · вьюпорт',
    tech: 'CSS keyframes',
    duration: 'одноразовая или непрерывная',
    cost: '0 КБ',
    crawlerRisk: 'критический при разбиении на буквы скриптом',
    rule: `Бегущая строка, счётчик, проявление заголовка по словам. Разбиение на слова и буквы
  делается в разметке заранее, а не скриптом: <span> с исходным словом внутри. Заголовок
  обязан читаться как одна строка текста при снятой анимации.`,
  },
  {
    key: 'svg',
    group: 'decor',
    name: 'SVG-анимация',
    trigger: 'вьюпорт · hover',
    tech: 'stroke-dasharray, CSS на путях',
    duration: 'одноразовая',
    cost: '0 КБ',
    crawlerRisk: 'нет',
    rule: `Отрисовка контура через stroke-dasharray и stroke-dashoffset, лёгкий морфинг форм.
  SVG инлайновый, со <title> для доступности. Lottie не подключается: это чужой JSON и
  ещё одна библиотека ради того, что рисуется путями.`,
  },
  {
    key: 'imagehover',
    group: 'decor',
    name: 'Наведение на изображения',
    trigger: 'hover',
    tech: 'clip-path, mask, filter',
    duration: 'реактивная',
    cost: '0 КБ',
    crawlerRisk: 'нет',
    rule: `Кадр отвечает на наведение: раскрытие маской через clip-path, снятие blur, мягкий
  зум внутри рамки, подпись, выезжающая снизу. Кадр не дёргается и не скачет по сетке.`,
  },
  {
    key: 'swap',
    group: 'decor',
    name: 'Смена двух кадров',
    trigger: 'вьюпорт · hover · перетаскивание ручки',
    tech: 'CSS keyframes на opacity, clip-path, <input type="range">',
    duration: 'непрерывная или реактивная',
    cost: '0 КБ',
    crawlerRisk: 'нет, пока оба кадра лежат в разметке',
    rule: `Два РАЗНЫХ снимка одного места, предмета или работы сменяют друг друга в одной рамке.
  Формы две, на страницу берётся одна и ровно в одном месте:

  ПЕРЕТЕКАНИЕ. Оба <img> лежат в рамке position:relative друг на друге, второй сверху с
  opacity 0. Цикл объявляется своим именем --loop-swap от 6s, анимируется ТОЛЬКО opacity:
    @keyframes swap { 0%,42% {opacity:0} 58%,100% {opacity:1} }
    .swap img + img { opacity: 0; transition: opacity var(--dur-modal) var(--ease-scene);
                      animation: swap var(--loop-swap) var(--ease-scene) infinite alternate; }
  На hover показывается второй кадр, и снимается ИМЕННО анимация:
    .swap:hover img + img { animation: none; opacity: 1; }
  animation-play-state: paused здесь не работает: работающая анимация перебивает обычное
  объявление opacity, и наведение замораживает случайную фазу вместо второго кадра.
  Так показывают «то же место с другой стороны» или «то же изделие в двух видах».

  ДО / ПОСЛЕ. Верхний кадр обрезан по горизонтали, положение реза — в CSS-переменной:
    .ba img + img { clip-path: inset(0 calc(100% - var(--pos, 50%)) 0 0); }
  Ручка — обычный <input type="range" min="0" max="100" value="50"> поверх рамки, с
  aria-label; вся логика одной строкой: oninput меняет --pos на процент. Подписи «до» и
  «после» лежат текстом в разметке, а не только в картинке.

  ЖЁСТКОЕ. Оба <img> всегда в HTML, с настоящими alt по содержимому кадра: приём не имеет
  права прятать второй кадр от поиска и от нейросетей. Кадры одного размера, одной точки
  съёмки и одного света — иначе смена читается как подмена, а не как одно и то же.
  «До/после» ставится ТОЛЬКО на снимки, которые дал сам клиент: чужая работа под подписью
  «до/после» — выдуманный факт о бизнесе и брак. Нет пары снимков — приём не ставится вовсе.
  При prefers-reduced-motion перетекание выключено и виден первый кадр, ручка «до/после»
  остаётся рабочей: она движется рукой человека, а не сама.`,
  },
  {
    key: 'cursor',
    group: 'decor',
    name: 'Курсорные эффекты',
    trigger: 'позиция курсора',
    tech: 'CSS-переменные + pointermove',
    duration: 'непрерывная',
    cost: '0 КБ',
    crawlerRisk: 'нет',
    rule: `Магнитная кнопка, подсветка-пятно за курсором, увеличенный курсор над кадрами.
  Только на устройствах с мышью — под @media (hover: hover) и (pointer: fine); на тач-экране
  не существует.`,
  },
  {
    key: 'ambient',
    group: 'decor',
    name: 'Атмосферный фон',
    trigger: 'постоянно',
    tech: 'CSS keyframes на градиентах',
    duration: 'непрерывная',
    cost: '0 КБ',
    crawlerRisk: 'нет',
    rule: `Медленное дыхание фона: перетекающий градиент, зерно, плавающая форма. Цикл длинный —
  от 8 секунд, амплитуда малая. Быстрое непрерывное движение утомляет и мешает читать.`,
  },
  {
    key: 'physics',
    group: 'decor',
    name: 'Физика и пружины',
    trigger: 'hover · drag',
    tech: 'cubic-bezier с перелётом, linear() для пружины',
    duration: 'реактивная',
    cost: '0 КБ',
    crawlerRisk: 'нет',
    rule: `Пружинный отклик вместо линейного: перелёт и возврат на кривой linear(), инерция
  при перетаскивании, мягкая деформация формы. Библиотека физики не подключается.`,
  },
  {
    key: 'data',
    group: 'decor',
    name: 'Оживление данных',
    trigger: 'вьюпорт',
    tech: 'CSS keyframes на высоте и counter',
    duration: 'одноразовая',
    cost: '0 КБ',
    crawlerRisk: 'критический — число обязано быть текстом',
    rule: `Рост столбцов и набор счётчиков при подходе к экрану. Итоговое число написано
  текстом в разметке, анимация только доводит до него. Числа берутся из описания бизнеса
  и ниоткуда больше.`,
  },
  {
    key: 'generative',
    group: 'decor',
    name: 'Генеративный холст',
    trigger: 'загрузка · курсор',
    tech: 'Canvas 2D своими руками',
    duration: 'непрерывная',
    cost: '0 КБ',
    crawlerRisk: 'нет, пока холст — фон',
    rule: `Один <canvas> как фон или сосед первого экрана: поле частиц, шум, линии за курсором.
  Кадр останавливается, когда холст вне экрана и когда вкладка скрыта; devicePixelRatio не
  выше 2; на ширине до 640px вместо холста статичный градиент того же настроения.`,
  },
  {
    key: 'product',
    group: 'decor',
    name: 'Продукт в действии',
    trigger: 'прогресс скролла · постоянно',
    tech: 'инлайновый SVG, clip-path, offset-path, stroke-dasharray',
    duration: 'управляемая прогрессом + непрерывная',
    cost: '0 КБ',
    crawlerRisk: 'нет при условии, что подписи всех стадий лежат в разметке',
    rule: `Продукт — герой сцены, и он ЖИВОЙ, а не статичный.

  ЧЕМ ИЗОБРАЖЁН ПРОДУКТ. Есть в задании список фотографий или видео — продукт это ТОЛЬКО
  настоящий снимок или кадр съёмки. Рисованных объектов на такой странице НЕТ НИ ОДНОГО:
  ни банки, ни чашки, ни струи, ни пчелы, ни капли, ни блика путями. Нарисованное рядом с
  фотографией читается как поломка вёрстки, и это уже проверено на живой странице.
  Инлайновый SVG остаётся только для интерфейса: стрелка, галочка, иконка в кнопке.

  ДВИЖЕТСЯ САМА ФОТОГРАФИЯ, а не рисунок поверх неё:
  - медленное приближение кадра циклом --loop-breath от 8s, масштаб от 1 до 1.06 — продукт «дышит»;
  - параллакс: продукт и фон едут с разной скоростью по animation-timeline: scroll();
  - раскрытие кадра маской clip-path по мере прокрутки — снимок словно наливается в рамку;
  - перетекание двух РАЗНЫХ снимков одного продукта: соты сменяются банкой, банка — ложкой
    с мёдом. Именно смена настоящих кадров даёт ощущение, что продукт меняется;
  - тень под продуктом углубляется, когда он приближается.
  Никаких искусственных струй и брызг: чего нет на снимке, того на странице нет.

  ЕСТЬ ВИДЕО — оно главнее фотографии: продукт живёт настоящей съёмкой, а прокрутка ведёт
  время видео. Тогда снимки уходят в карточки и полосы между секциями.

  СЦЕНА ВЕДЁТСЯ СКРОЛЛОМ, И В КАДРЕ ВСЕГДА ОДНА СТАДИЯ. Секция закрепляется на sticky,
  продукт проходит 3–4 стадии, рядом сменяются короткие фразы — по одной на стадию.
  Стадии лежат друг над другом в одной точке и переключаются по прогрессу скролла:
  видна ровно одна, остальные с opacity 0 ПОД КЛАССОМ .js. Все стадии, показанные
  одновременно, — главный способ превратить сцену в кашу из наложенных фигур; проверь
  перед выдачей, что в каждый момент прокрутки читается один кадр, а не все сразу.
  Тексты всех стадий лежат в разметке подряд и читаются без скрипта.

  РОЙ ВОКРУГ ПРОДУКТА — ТОЛЬКО когда фотографий и видео в задании нет вовсе. Тогда 5–9
  мелких нарисованных объектов ниши летают по РАЗНЫМ кривым: каждому свой offset-path,
  offset-distance от 0% до 100%, длительность своим --loop-fly от 6s, задержка своя,
  направление у части обратное; крылья трепещут по --dur-hover; размер не больше --rise.
  Одинаковые траектории
  и синхронный полёт — брак, это читается как гирлянда. На ширине до 640px рой сокращается
  до трёх. Есть настоящие кадры — роя нет: он всегда проигрывает съёмке.

  Вся сцена помечена aria-hidden="true"; при prefers-reduced-motion продукт показан в
  финальной стадии, рой не летает.`,
  },
  {
    key: 'character',
    group: 'decor',
    name: 'Персонаж за работой',
    trigger: 'постоянно · hover · курсор',
    tech: 'инлайновый SVG, группы с собственным transform-origin, CSS keyframes',
    duration: 'непрерывная',
    cost: '0 КБ',
    crawlerRisk: 'нет — сцена декоративная, смысл продублирован текстом',
    rule: `Нарисованная сцена, в которой герой ниши делает своё дело прямо на первом экране, и
  она не останавливается: грумеру — собака под феном, шерсть развевается волнами и отдельные
  волоски улетают по дуге; парикмахеру — ножницы стригут прядь и срезанные волосы падают;
  кондитеру — мешок отсаживает крем кольцами на торт; автомойке — пена сползает по капоту.
  Сцена — инлайновый SVG, никаких картинок по ссылке.

  ЧТО ДЕЛАЕТ СЦЕНУ ЖИВОЙ, а не дёргающейся картинкой — соблюдай всё:
  - Части тела разнесены по группам <g> со СВОИМ transform-origin: корпус, голова, уши,
    хвост, лапы, шерсть, инструмент. Одна анимация на весь рисунок — мёртвый результат.
  - Циклы РАЗНОЙ длительности и не кратные друг другу: дыхание корпуса 3.2s, уши 1.7s,
    хвост 0.9s, инструмент 2.3s. Совпали периоды — сцена читается как механизм.
  - Вторичное движение запаздывает: уши, шерсть и хвост повторяют движение корпуса с
    задержкой 80–160ms. Это главное, что отличает живое от анимированного.
  - Моргание редкое и короткое: в keyframes глаза закрыты между 96% и 99%, цикл 5–7s.
  - Поток от инструмента — 6–9 линий разной длины с разными задержками и длительностями,
    иначе получается гребёнка. Частицы (шерсть, брызги, крошка) летят по дуге через
    translate плюс rotate, тают к концу пути, задержки не совпадают.
  - Амплитуды маленькие: 2–6px и 1–4 градуса. Большие сдвиги превращают персонажа в
    резиновую игрушку.
  - Реакция на человека: при наведении герой отзывается — жмурится, поворачивает голову,
    виляет быстрее. Одна реакция, не пять.

  Сцена помечена aria-hidden="true", рядом текстом сказано то же самое словами.
  При prefers-reduced-motion сцена замирает в удачной позе: инструмент выключен,
  частицы не летят, персонаж просто нарисован.`,
  },
  {
    key: 'signature',
    group: 'decor',
    name: 'Фирменная сцена ниши',
    trigger: 'прогресс скролла · hover',
    tech: 'инлайновый SVG + transform на animation-timeline: scroll()',
    duration: 'управляемая прогрессом',
    cost: '0 КБ',
    crawlerRisk: 'нет — сцена декоративная, текст рядом',
    rule: `Один запоминающийся объект самой ниши, нарисованный инлайновым SVG и оживающий по
  скроллу. Объект выводится из описания бизнеса, а не берётся из общего набора: мебельщику —
  шкаф, у которого разъезжаются двери и выдвигаются ящики, обнажая полки; автосервису —
  машина, у которой отделяется колесо и видно подвеску; пекарю — печь с открывающейся
  дверцей и поднимающимся хлебом; юристу — стопка документов, разлетающаяся в веер.
  Механика: секция закрепляется на position: sticky, объект внутри собирается или
  раскрывается по прогрессу скролла через animation-timeline: scroll(); двери едут на
  transform-origin у петли, ящики — на translateX, внутренности проявляются с задержкой.
  Диапазон обязателен во всю закреплённую секцию: animation-range: entry 25% exit 25%
  либо scroll() по sticky-контейнеру. Диапазон вида «cover 60%» — брак: раскрытие
  закончится раньше, чем объект дойдёт до середины экрана, и человек увидит уже открытый
  шкаф вместо того, как он открывается. Смысл приёма именно в том, что раскрытие идёт,
  пока объект перед глазами.
  Обязательно: сцена отыгрывает назад при обратной прокрутке, работает на любой скорости,
  помечена aria-hidden="true", а рядом лежит текст, который несёт тот же смысл словами.
  При prefers-reduced-motion объект показан в раскрытом состоянии сразу.
  Ровно одна такая сцена на страницу — вторая обесценивает первую.`,
  },
  {
    key: 'webgl',
    group: 'decor',
    name: 'WebGL и 3D',
    trigger: 'загрузка · курсор · скролл',
    tech: 'Three.js по ESM-импорту',
    duration: 'непрерывная',
    cost: '~150 КБ',
    crawlerRisk: 'критический, если сценой рисуется контент',
    rule: `Трёхмерная сцена подключается ТОЛЬКО ради настоящей геометрии, одним ESM-импортом
  Three.js с зафиксированной версией через import map. Сцена — слой поверх готовой страницы:
  убери <canvas> из документа, и страница обязана остаться законченной. Старт после отрисовки
  текста, остановка вне экрана, отключение при prefers-reduced-motion и на ширине до 900px.`,
  },
  {
    key: 'easter',
    group: 'decor',
    name: 'Эмоциональные мелочи',
    trigger: 'событие · клик',
    tech: 'CSS keyframes',
    duration: 'одноразовая',
    cost: '0 КБ',
    crawlerRisk: 'нет',
    rule: `Одна человеческая мелочь: отклик после отправки формы, живая иконка, оживший 404,
  мягкое переключение тёмной темы. Ровно одна на страницу — иначе это шум, а не характер.`,
  },
];

export const PRESETS = [
  {
    key: 'off',
    name: 'Покоя — движения нет',
    reference: '—',
    when: 'печатные и документные страницы, высокий порог доверия, медленная мобильная сеть',
    techniques: [],
    tail: `Разрешены только смены цвета и подчёркивания на hover и focus-visible, по --dur-hover,
без трансформаций и без появления по скроллу. Класса .reveal и opacity:0 в CSS быть не должно.`,
  },
  {
    key: 'interface',
    name: 'Интерфейс — движется то, чего касается человек',
    reference: 'rauno.me',
    when: 'страницы записи и расчёта, услуги, личные страницы специалистов: ценна аккуратность, а не зрелищность',
    techniques: ['micro', 'state', 'feedback', 'nav', 'easter'],
    tail: 'Появления по скроллу нет: секции нарисованы сразу.',
  },
  {
    key: 'landing',
    name: 'Лендинг — страница разворачивается по мере чтения',
    reference: 'hellomonday.com',
    when: 'значение по умолчанию: посадочная страница бизнеса',
    techniques: ['micro', 'state', 'feedback', 'nav', 'intro', 'gallery', 'reveal', 'kinetic', 'imagehover', 'swap', 'easter'],
    tail: `Ритм важнее количества: движение живёт на первом экране, на смене секций и на одном
запоминающемся приёме. Анимированное всё подряд читается как дешёвый шаблон.`,
  },
  {
    key: 'story',
    name: 'История — скролл ведёт повествование',
    reference: 'darkroom.engineering',
    when: 'портфолио, студия, длинная страница услуги, где важен порядок раскрытия',
    techniques: ['micro', 'state', 'nav', 'intro', 'gallery', 'reveal', 'parallax', 'pinned', 'progress', 'story', 'kinetic', 'imagehover', 'swap', 'cursor', 'physics', 'signature', 'product'],
    tail: 'Закреплённая сцена на странице одна: две подряд превращают чтение в борьбу со скроллом.',
  },
  {
    key: 'showcase',
    name: 'Витрина — живой холст поверх обычной страницы',
    reference: 'mont-fort.com, lusion.co',
    when: 'бизнес продаёт впечатление и материал: премиальный товар, архитектура, трейдинг, студия',
    techniques: ['micro', 'state', 'nav', 'intro', 'gallery', 'reveal', 'parallax', 'pinned', 'kinetic', 'imagehover', 'swap', 'cursor', 'ambient', 'physics', 'generative', 'signature', 'product', 'webgl'],
    tail: `Так устроен mont-fort.com: WebGL сверху, а в HTML лежат 7559 знаков текста, которые
читают и поиск, и нейросети. Убери <canvas> — страница обязана остаться законченной.`,
  },
  {
    key: 'world',
    name: 'Мир — страница и есть сцена',
    reference: 'bruno-simon.com, activetheory.net',
    when: 'портфолио и шоукейс, где сайт сам себе демонстрация. Для страницы, которую должны находить в поиске, пресет не годится',
    techniques: ['micro', 'nav', 'loading', 'cursor', 'physics', 'generative', 'webgl'],
    tail: `Обязательны шкала загрузки, экранная подсказка управления и видимый выход к текстовой
версии страницы. На ширине до 900px и при prefers-reduced-motion показывается текстовая версия,
а не заглушка «откройте с компьютера».
Цена ошибки измерена: activetheory.net отдаёт машине 69 знаков текста — нейросети цитировать
нечего. bruno-simon.com на том же уровне держит в HTML 3130 знаков и десяток заголовков;
второй способ — правильный.`,
  },
];

export const DEFAULT_PRESET = 'landing';

const SCALE = `ШКАЛА ДВИЖЕНИЯ — единственный источник чисел, объявляется в :root первым делом:
  --dur-hover: 120ms;      наведение и мелкий отклик
  --dur-ui: 200ms;         тултип, выпадашка, аккордеон, вкладка
  --dur-modal: 280ms;      модальное окно и шторка
  --dur-reveal: 500ms;     появление блока при прокрутке
  --dur-hero: 700ms;       первый экран
  --stagger: 80ms;         пауза между соседями в каскаде
  --dur-ui-out: 160ms;     уход с экрана на 20% быстрее прихода
  --dur-modal-out: 220ms;
  --ease-ui: cubic-bezier(0.165, 0.84, 0.44, 1);      интерфейс и входы
  --ease-scene: cubic-bezier(0.4, 0, 0.2, 1);         крупное и сценовое
  --ease-move: cubic-bezier(0.645, 0.045, 0.355, 1);  перемещение по экрану
  --rise: 24px;            сдвиг при появлении
  --press: 0.96;           сжатие кнопки при нажатии

ДВЕ ШКАЛЫ НЕ СМЕШИВАЮТСЯ. То, чего человек касается — кнопка, ссылка, меню, аккордеон, —
живёт на --dur-hover и --dur-ui. То, что он разглядывает — первый экран и появление
блоков, — на --dur-hero и --dur-reveal. Медленная кнопка читается как тормоз, быстрый
первый экран — как дешёвый шаблон.

Ни одного времени и ни одной кривой мимо этих имён: в transition и animation стоят
var(--dur-…) и var(--ease-…), а не «300ms» и не «ease-in-out». Отскока и перелёта в
кривых нет — отскок это «весело», а не «дорого».
Исключение ровно одно: непрерывные декоративные циклы (дыхание фона, смена кадров,
бегущая строка) объявляются своими именами --loop-… с длительностью от 6s — к шкалам
отклика они не относятся.

Сдвиг при появлении — не больше --rise: движение через полэкрана выглядит дёшево.
Одновременно двигаются максимум три элемента, соседи разводятся через --stagger.
Частое действие не анимируется или берёт нижний край шкалы: то, что жмут десятки раз
за визит, красивым медленным раскрытием превращается в раздражение.
Подвал не анимируется вовсе.`;

const CONTENT_FIRST = `ИНВАРИАНТ ДВИЖЕНИЯ (сильнее любого приёма выше):
Весь смысловой текст лежит в HTML и виден без единого выполненного скрипта. Движение —
слой поверх готовой страницы, а не способ её собрать. Мысленно удали из документа все
<script> и <canvas>: каждая секция — услуги, шаги, вопросы, контакты — обязана остаться
на месте. Пропал хоть один блок — переделай.`;

const REVEAL_GUARD = `ПРАВИЛО СКРЫТИЯ (иначе половина сайта окажется невидимой):
Любое скрытие ради анимации разрешено ТОЛЬКО под классом .js на <html>, который ставит
инлайновый скрипт первой строкой в <head>:
  <script>document.documentElement.classList.add('js')</script>
и в CSS:
  .reveal { opacity: 1; }
  .js .reveal { opacity: 0; transform: translateY(var(--rise));
                transition: opacity var(--dur-reveal) var(--ease-scene),
                            transform var(--dur-reveal) var(--ease-scene);
                transition-delay: calc(var(--i, 0) * var(--stagger)); }
  .js .reveal.is-in { opacity: 1; transform: none; }
Появление по прокрутке ведёт встроенный в браузер наблюдатель, скриптом в конце <body>:
  <script>
    const w = new IntersectionObserver((es) => {
      for (const e of es) if (e.isIntersecting) { e.target.classList.add('is-in'); w.unobserve(e.target); }
    }, { rootMargin: '0px 0px -10% 0px', threshold: 0.12 });
    for (const el of document.querySelectorAll('.reveal')) w.observe(el);
  </script>
Номер соседа в каскаде задаётся инлайновым style="--i:0", style="--i:1", style="--i:2" —
так карточки внутри блока выходят по очереди, а не разом.

animation-timeline: view() НЕ использовать. Там, где свойство не поддержано, оно молча
игнорируется, а сама animation отыгрывает по обычной шкале — то есть сразу при загрузке.
Тогда вся страница проявляется разом, каскада нет вовсе, и по коду этого не видно:
и вёрстка, и сборка остаются зелёными.

Базовое правило .reveal — БЕЗ opacity:0 и без скрывающих transform: элемент виден всегда.
Тогда там, где скрипт не выполнился и где страница не прокручивается — краулер нейросети,
режим чтения, печать, скриншот целой страницы — весь текст на месте.

Правило распространяется на ВСЕ селекторы, а не только на .reveal. Ступенчатый вход
первого экрана делается так же: базовое правило видимое, скрытие и задержка — под .js,
и там уместна обычная CSS-анимация по времени, без наблюдателя: первый экран виден сразу.
  .hero .lead { opacity: 1; }                        ← правильно
  .js .hero .lead { opacity: 0; animation: rise-in var(--dur-hero) var(--ease-scene) both;
                    animation-delay: calc(var(--i, 0) * var(--stagger)); }
  .hero .lead { opacity: 0; }                        ← БРАК, текст пропал без скриптов

САМОПРОВЕРКА ПЕРЕД ВЫДАЧЕЙ, обязательная: найди в своём CSS каждое вхождение opacity:0,
opacity: 0, visibility:hidden и скрывающий transform (translate больше 20px, scale меньше 1).
У каждого такого правила селектор ОБЯЗАН начинаться с .js — либо элемент скрыт по смыслу,
а не ради анимации (мобильная панель, подсветка за курсором). Нашёл хоть одно исключение —
перепиши правило, а не оставляй как есть.`;

const PERFORMANCE = `ДВА ПРАВИЛА, КОТОРЫЕ НАРУШАЮТ ЧАЩЕ ВСЕГО:
- Анимируются только transform и opacity. Ширина, высота, отступы, top/left в анимации
  запрещены: каждый кадр пересчитывает раскладку и страница дёргается.
- Два медиа-блока обязательны оба, дословно, и оба в конце <style>:
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after { animation: none !important; transition: none !important; }
      .js .reveal { opacity: 1 !important; transform: none !important; }
    }
    @media print {
      .js .reveal { opacity: 1 !important; transform: none !important; animation: none !important; }
      canvas { display: none; }
    }
  Первый — потому что у части людей от параллакса и больших сдвигов реально кружится голова.
  Второй — потому что на печати и в PDF анимация не сработает, и без него страница выйдет
  из принтера с пустыми блоками. Отсутствие любого из двух — брак.`;

const NO_LIBS = `Библиотеки анимации не подключаются: GSAP, ScrollTrigger, Lenis, Framer Motion,
Lottie и AOS запрещены. Всё перечисленное делается на CSS. Единственное исключение —
Three.js на пресетах «витрина» и «мир», и только ради настоящей трёхмерной геометрии.`;

export function pickPreset(key) {
  const wanted = String(key ?? '').trim().toLowerCase();
  return PRESETS.find((p) => p.key === wanted) ?? PRESETS.find((p) => p.key === DEFAULT_PRESET);
}

export function resolveTechniques(preset, { add = [], drop = [] } = {}) {
  const keys = new Set([...preset.techniques, ...add.map((k) => String(k).trim().toLowerCase())]);
  for (const k of drop) keys.delete(String(k).trim().toLowerCase());
  return TECHNIQUES.filter((t) => keys.has(t.key));
}

export function motionBlock(preset, options = {}) {
  const chosen = resolveTechniques(preset, options);

  if (!chosen.length) {
    return `АНИМАЦИЯ — пресет «${preset.name}»: движения на странице нет.\n${preset.tail}\n\n${SCALE}\n\n${PERFORMANCE}`;
  }

  const byGroup = MOTION_GROUPS.map((g) => {
    const items = chosen.filter((t) => t.group === g.key);
    if (!items.length) return '';
    const lines = items.map((t) => `- ${t.name} (${t.trigger}): ${t.rule}`).join('\n');
    return `${g.title}:\n${lines}`;
  })
    .filter(Boolean)
    .join('\n\n');

  const needsRevealGuard = chosen.some((t) => ['intro', 'reveal', 'pinned', 'story', 'kinetic', 'data'].includes(t.key));

  return [
    `АНИМАЦИЯ — пресет «${preset.name}» (референс: ${preset.reference}).`,
    'Делай перечисленные приёмы и не добавляй других: набор задан заранее.',
    SCALE,
    byGroup,
    preset.tail,
    NO_LIBS,
    CONTENT_FIRST,
    needsRevealGuard ? REVEAL_GUARD : '',
    PERFORMANCE,
  ]
    .filter(Boolean)
    .join('\n\n');
}
