---
type: handoff
feature: site-pipeline
date: 2026-08-12
from: tz-author
to: tz-orchestrator
next_agent_entrypoint: true
blocked_by: решение владельца о деньгах на Фазу 0 (0.3798 USD за 20 входов)
---

# Handoff: реализация ТЗ site-pipeline

## Промпт для следующей сессии

> Реализуй ТЗ `plans/tz/2026-08-12-site-pipeline.md` фазами через `tz-orchestrator`.
> Порядок чтения: `CLAUDE.md` → `.claude/CLAUDE.md` → `plans/architecture/2026-08-11-site-pipeline.md`
> (одобрена, `status: approved`) → само ТЗ → код-якоря `tools/probe-site-gen.mjs:138` и `:152`,
> `tools/check-page.mjs`.
> Граф фаз: Фаза 0 строго первая (даёт число порога), затем 1 и 3 в любом порядке, Фаза 2 после нуля.
> Коммит на каждую зелёную фазу.

## Мандат владельца — НЕ переоткрывать

| # | Решение | Дата |
|---|---|---|
| В1 | Архитектура конвейера одобрена | 2026-08-12 |
| В2 | Один сборщик + петля проверки; агентов-ролей не заводим (развилка Р1) | 2026-08-12 |
| В3 | Ручной просмотр 20 страниц пробы ядра снят, задача в архиве | 2026-08-12 |

Владелец разбирал Р1 отдельно и подробно, возражение агента высказано дважды и снято.
Возвращаться к схеме «начальник + копирайтер + верстальщик + SEO» — нарушение мандата.

## Чем звено заблокировано

Фаза 0 требует прогона 20 входов текущим генератором. Предыдущий такой прогон стоил
**0.3798 USD** суммарно, медиана 120 с на страницу. Деньги — решение владельца (§1).
Слово «прогоняй» снимает блок; всё остальное в ТЗ от этого решения не зависит —
Фазы 1 и 3 стартуют без него.

## Грабли, которые уже стоили времени

- Артефакты `artifacts/core-probe/site-gen/*.html` от 10.08.2026 замерены **до** коммита
  `852194c`. Строка «доля годных 0 из 20» в `result.md` означает «не судили», а не «модель
  не смогла»: 19 страниц из 20 имеют ровно два одинаковых дефекта, оба уже устранены.
  Порог с этих артефактов брать нельзя.
- `probe-site-gen.mjs:152` — готовая точка расширения (`dir.structure ?? DEFAULT_STRUCTURE`).
  Тело файла переписывать не нужно, номер строки перед правкой перечитать.
- `playwright@^1.62.1` уже стоит в `devDependencies` — новую зависимость под скриншоты не заводить.

## Границы

Не делать: многостраничник, hreflang и EN-версию, загрузку файлов клиента, дообучение модели,
агентов-ролей, переписывание `check-page.mjs`.
