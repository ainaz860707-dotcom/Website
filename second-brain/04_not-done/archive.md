# Закрытые строки реестра — архив

> Что было и чем закрыто. Полный текст каждой строки хранит git; сюда переносится одна строка.
> Перед задачей этот файл читать не нужно — он про прошлое, а не про открытые пробелы.

| Дата закрытия | Что было | Чем закрыто |
|---|---|---|
| 2026-08-10 | «Страница Григория Борисова вылезает по горизонтали на 390px» | Находка ложная. Замер в живом браузере: `scrollWidth` 390 = `clientWidth` 390, вылета нет. За край выходит только `.marquee-track` внутри `.marquee{overflow:hidden}` — так и задумано. Обрезанный текст был артефактом headless-скриншота, а не дефектом страницы |
| 2026-08-10 | Пустая колонка рядом с шагами в разделе «Как проходит съёмка» | Лишний `<div></div>` в каждом `<li>`: номер шага рисует `li::before` и он уже первая ячейка сетки `5rem 1fr`, пустой div занимал вторую, текст уезжал на новую строку. Div убран, проверено в браузере — номер и текст в одной строке |
| 2026-08-11 | На пресете `showcase` модель прятала содержимое без скриптов: `opacity:0` в базовых правилах `.hero .lead`, `.hero-actions`, `.facts-row` | Правило скрытия в промпте распространено на все селекторы и снабжено обязательной самопроверкой по каждому `opacity:0`; добавлена автопроверка `tools/check-page.mjs`, она же нашла два из трёх. Коммит `852194c`, перегенерация чистая |
| 2026-08-11 | Сломанная раскладка закреплённой сцены «Три шага»: абзац уезжал в колонку под номером и вставал по одному слову в строку | Причина — `::before` первой ячейкой сетки и два элемента на две колонки. В промпт добавлено правило подсчёта ячеек; модель обернула содержимое шага в один `<div>`, проверено скриншотом со скроллом. Коммит `852194c` |
| 2026-08-11 | Отсутствие `@media print` в выдаче генератора | Оба медиа-блока (`prefers-reduced-motion` и `print`) заданы в промпте дословно кодом вместо описания словами; наличие обоих проверяет `check-page.mjs`. Коммит `852194c` |
| 2026-08-12 | Проба ядра `site-gen` не отсужена: 20 страниц сгенерированы, вердикт по критерию не проставлен | Снято решением владельца 2026-08-12: ручной просмотр 20 страниц не делаем. Строка «доля годных 0 из 20» в `result.md` означает «не судили», а не «модель не смогла» |
| 2026-08-12 | `stock-video.mjs` ни к чему не подключён: видео на первом экране генератор не делает | Причина в строке («нужна папка ассетов, а выдача — один `.html`») оказалась снятой без смены формата: видео и постер живут по внешним адресам, в HTML уходит только ссылка. Слой подключён в `probe-site-gen.mjs` за рубильником `SKIP_VIDEO`, рецепт скролл-скраббинга уходит в промпт кодом, семь правил считает `check-page.mjs`. Осталось прогнать живьём — новая строка в реестре |
| 2026-08-12 | Ось «поверхность» и шкала движения проверены только на синтетике: живого прогона генератора не было | Закрыто прогоном `PROBE_SLUG=motion-scale` на входе «пекарня на закваске в Кургане» (0.6704 USD): на выданной странице поверхность собрана слоями с зерном (`feTurbulence` + два радиальных градиента), вся шкала стоит в `:root`, сырых времён и кривых вне `:root` — ноль, каскад идёт наблюдателем (5→7→11→15→21 из 31 блока по мере прокрутки) |

| 2026-08-12 | Awwwards не подключён к `tools/reference-sources.mjs`; не проверено, дают ли тематические разделы разные списки | Закрыто по решению владельца назвать Awwwards и Dribbble источниками доски. Разделы сравнены: `architecture` 32 · `e-commerce` 33 · `photography` 32 ссылки, пересечение 1 из 32 — списки разные. Подключены `awwwardsSites()` и `dribbbleShots()`, оба без сессии; на доске флаг `--refs` |
| 2026-08-12 | Стоковая ветка слоя видео не прогнана живьём: неизвестно, какие сцены Pexels отдаёт по русскому описанию бизнеса | Закрыто прогоном с ключом владельца: вход «свадебный фотограф в Алматы, репортажная съёмка» → запросы `wedding couple` и `wedding bouquet`, 3 клипа по 8–10с, все в нише. Вскрыто побочно: `bestFile` берёт самый лёгкий файл ≥960px, на первый экран приходит 960×540 |
| 2026-08-13 | `page-critic.mjs` режет скриншот на 6000px и не видит нижнюю половину страницы | Строка протухла: нарезка сделана коммитом `fb28e82`, потолок 16 экранов — `9adf9d3`. Замер на `artifacts/core-probe/gallery-story/000.html`: высота 8703px при экране 844 → 11 кадров на 390px, 7655px при экране 1000 → 8 кадров на 1440px, потолок не задет. Последний кадр `1440-08.png` — блок контактов целиком: «Город: Алматы» и плейсхолдеры ТЕЛЕФОН/МЕССЕНДЖЕР видны, то есть оси «город» и «плейсхолдеры» критик снимает с низа страницы |
| 2026-08-13 | Ролик Higgsfield не сгенерирован: на триале `generate create` отвечал `only_mcp_usage_on_trial_is_available` | Закрыто прогоном через MCP-канал: `generate_video_batch` на `kling3_0`, 4 клипа (две сцены × 16:9 и 9:16), `mode: pro`, `sound: off`, 5с — списано 35 кредитов из 110, остаток 75. Цена сверена до прогона через CLI: `generate cost` = 8.75 кредита за клип. Файлы и вёрстка — `artifacts/hero-video/` |

[[README|← реестр не-сделанного]]
